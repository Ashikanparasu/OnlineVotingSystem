package com.votemate.service;

// This file was previously misplaced and duplicated the repository interface.
// Keep a placeholder here (different package & name) so it doesn't conflict with the real repository.

import org.springframework.stereotype.Component;

@Component
class VoteRecordRepositoryHelper {
	// intentionally empty - placeholder helper to avoid accidental imports from 'service' package
}
